services:
  heimdall:
    image: linuxserver/heimdall:2.8.1
    container_name: heimdall
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/Berlin
    volumes:
      - ./config:/config
    ports:
      - 80:80
      - 443:443
